import React from "react";

function Header(){
    return(
        <header>
        <h1>Keeper</h1>
        <p>A React practice mini project on components and props with map function</p>
        </header>
    );
};
export default Header;